import {
    StyleSheet,
} from "react-native";

const menuCommonStyles = StyleSheet.create({
    navigationBarIconPadding: {
        paddingTop:9,
        paddingRight:7,
    },
});

export default menuCommonStyles;

